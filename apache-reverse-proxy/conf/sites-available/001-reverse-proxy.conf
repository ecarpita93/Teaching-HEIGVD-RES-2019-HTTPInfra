<VirtualHost *:80>
    ServerName demo.res.ch
	
	# HARD CODED ADRESSES, VERIFY BEFORE LAUNCHING
	
    # First rule, to get the ex_dynamic data
    ProxyPass "/api/animals/" "http://172.17.0.2:3000/"
    ProxyPassReverse "/api/animals/" "http://172.17.0.2:3000/"

    # Generic rule, will charge the static page
    ProxyPass "/" "http://172.17.0.3:80/"
    ProxyPassReverse "/" "http://172.17.0.3:80/"

</VirtualHost>